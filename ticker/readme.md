# how to use it

* add `tickers.py` with two lines
    * tickers = ["ticker1", ...] # example tickers = ["GOOGL", ...]
    * raw_weights = [weight1, ...] # example raw_weights = [33.15, ...] # should not need to add up to 100
* run pyhon3 ticker_compare_vX.py #use latest looking ticker_compare file